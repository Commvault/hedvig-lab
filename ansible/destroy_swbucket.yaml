- name: Create Azure resources
  hosts: localhost
  vars_files:
    - ./vars.yaml
  connection: local
  tasks:
    - name: Destroy Azure resource group, bucket to store software
      terraform:
        project_path: "{{ lookup('env', 'PWD') }}/../terraform/azure/swbucket/"
        state: absent